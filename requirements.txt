# TradeNet Fabric — Python Dependencies
# Network automation and tooling

# SDN controller — REST API (Dijkstra path computation + topology state)
flask>=3.0            # HTTP API for the SDN controller (port 9090)

# Ansible — declarative network configuration management
ansible>=8.0
ansible-pylibssh>=1.1.0

# Ansible network collections
# (installed via ansible-galaxy, listed here for reference)
# ansible.netcommon
# cisco.ios
# arista.eos
# junipernetworks.junos

# Nornir — programmatic network automation
nornir>=3.4
nornir-napalm>=0.4
nornir-netmiko>=1.0
nornir-utils>=0.2

# Network libraries
napalm>=4.1           # Multi-vendor network abstraction
netmiko>=4.2          # SSH connections to network devices
paramiko>=3.3         # SSH transport
pyeapi>=1.0           # Arista eAPI client
junos-eznc>=2.6       # Juniper PyEZ client
netaddr>=0.9          # IP address manipulation

# Configuration validation
yamllint>=1.32        # YAML linting
jinja2>=3.1           # Template engine (also used by Ansible)
jsonschema>=4.20      # Schema validation for config files

# Monitoring and metrics
prometheus-client>=0.19   # Prometheus metric exporter

# Chaos engineering
pyyaml>=6.0           # YAML parsing for chaos scenarios

# Testing
pytest>=7.4
pytest-cov>=4.1

# Utilities
rich>=13.7            # Terminal formatting for reports
tabulate>=0.9         # Table formatting for benchmark results
